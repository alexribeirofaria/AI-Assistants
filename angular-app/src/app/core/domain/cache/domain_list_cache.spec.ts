import { DomainListCache } from './domain_list_cache';

describe('DomainListCache', () => {
  it('should be instantiated', () => {
    const instance = new DomainListCache();
    expect(instance).toBeTruthy();
  });
});
