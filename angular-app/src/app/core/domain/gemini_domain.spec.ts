import { GeminiDomain } from './gemini_domain';

describe('GeminiDomain', () => {
  it('should be instantiated', () => {
    const instance = new GeminiDomain();
    expect(instance).toBeTruthy();
  });
});
