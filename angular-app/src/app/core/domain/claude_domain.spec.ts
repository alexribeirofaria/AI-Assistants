import { ClaudeDomain } from './claude_domain';

describe('ClaudeDomain', () => {
  it('should be instantiated', () => {
    const instance = new ClaudeDomain();
    expect(instance).toBeTruthy();
  });
});
