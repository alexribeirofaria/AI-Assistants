import { OutputStream } from './output_stream';

describe('OutputStream', () => {
  it('should be instantiated', () => {
    const instance = new OutputStream();
    expect(instance).toBeTruthy();
  });
});
