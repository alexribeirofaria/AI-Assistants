import { OutputPresenter } from './output_presenter';

describe('OutputPresenter', () => {
  it('should be instantiated', () => {
    const instance = new OutputPresenter();
    expect(instance).toBeTruthy();
  });
});
