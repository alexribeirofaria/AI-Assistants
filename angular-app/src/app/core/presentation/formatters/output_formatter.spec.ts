import { OutputFormatter } from './output_formatter';

describe('OutputFormatter', () => {
  it('should be instantiated', () => {
    const instance = new OutputFormatter();
    expect(instance).toBeTruthy();
  });
});
