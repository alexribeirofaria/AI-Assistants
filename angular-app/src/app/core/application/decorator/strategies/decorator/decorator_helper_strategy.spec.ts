import { DecoratorHelperStrategy } from './decorator_helper_strategy';

describe('DecoratorHelperStrategy', () => {
  it('should be instantiated', () => {
    const instance = new DecoratorHelperStrategy();
    expect(instance).toBeTruthy();
  });
});
