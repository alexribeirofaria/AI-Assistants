import { HelpStrategy } from './help_strategy';

describe('HelpStrategy', () => {
  it('should be instantiated', () => {
    const instance = new HelpStrategy();
    expect(instance).toBeTruthy();
  });
});
