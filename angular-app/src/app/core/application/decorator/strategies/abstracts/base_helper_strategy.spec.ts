import { BaseHelperStrategy } from './base_helper_strategy';

describe('BaseHelperStrategy', () => {
  it('should be instantiated', () => {
    const instance = new BaseHelperStrategy();
    expect(instance).toBeTruthy();
  });
});
