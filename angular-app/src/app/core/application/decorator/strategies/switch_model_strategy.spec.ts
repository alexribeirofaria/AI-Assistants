import { SwitchModelStrategy } from './switch_model_strategy';

describe('SwitchModelStrategy', () => {
  it('should be instantiated', () => {
    const instance = new SwitchModelStrategy();
    expect(instance).toBeTruthy();
  });
});
