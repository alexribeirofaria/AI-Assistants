import { ListModelsStrategy } from './list_models_strategy';

describe('ListModelsStrategy', () => {
  it('should be instantiated', () => {
    const instance = new ListModelsStrategy();
    expect(instance).toBeTruthy();
  });
});
