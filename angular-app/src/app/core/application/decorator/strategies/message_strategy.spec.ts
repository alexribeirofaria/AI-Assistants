import { MessageStrategy } from './message_strategy';

describe('MessageStrategy', () => {
  it('should be instantiated', () => {
    const instance = new MessageStrategy();
    expect(instance).toBeTruthy();
  });
});
