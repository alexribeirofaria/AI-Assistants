import { ExitStrategy } from './exit_strategy';

describe('ExitStrategy', () => {
  it('should be instantiated', () => {
    const instance = new ExitStrategy();
    expect(instance).toBeTruthy();
  });
});
