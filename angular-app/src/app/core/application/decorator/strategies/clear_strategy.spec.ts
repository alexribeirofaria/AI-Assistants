import { ClearStrategy } from './clear_strategy';

describe('ClearStrategy', () => {
  it('should be instantiated', () => {
    const instance = new ClearStrategy();
    expect(instance).toBeTruthy();
  });
});
