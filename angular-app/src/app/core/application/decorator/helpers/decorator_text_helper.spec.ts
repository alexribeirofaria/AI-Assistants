import { DecoratorTextHelper } from './decorator_text_helper';

describe('DecoratorTextHelper', () => {
  it('should be instantiated', () => {
    const instance = new DecoratorTextHelper();
    expect(instance).toBeTruthy();
  });
});
