import { AiAssistantApp } from './ai_assistant_app';

describe('AiAssistantApp', () => {
  it('should be instantiated', () => {
    const instance = new AiAssistantApp();
    expect(instance).toBeTruthy();
  });
});
