import { ThreadController } from './thread_controller';

describe('ThreadController', () => {
  it('should be instantiated', () => {
    const instance = new ThreadController();
    expect(instance).toBeTruthy();
  });
});
