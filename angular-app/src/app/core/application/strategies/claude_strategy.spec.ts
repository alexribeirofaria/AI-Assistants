import { ClaudeStrategy } from './claude_strategy';

describe('ClaudeStrategy', () => {
  it('should be instantiated', () => {
    const instance = new ClaudeStrategy();
    expect(instance).toBeTruthy();
  });
});
