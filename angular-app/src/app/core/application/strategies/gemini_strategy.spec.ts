import { GeminiStrategy } from './gemini_strategy';

describe('GeminiStrategy', () => {
  it('should be instantiated', () => {
    const instance = new GeminiStrategy();
    expect(instance).toBeTruthy();
  });
});
