import { BaseApplicationStrategy } from './base_application_strategy';

describe('BaseApplicationStrategy', () => {
  it('should be instantiated', () => {
    const instance = new BaseApplicationStrategy();
    expect(instance).toBeTruthy();
  });
});
