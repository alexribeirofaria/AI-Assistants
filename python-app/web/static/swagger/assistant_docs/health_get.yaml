swagger: '2.0'
info:
  title: Health Check
  version: 1.0.0
produces:
- application/json
paths:
  /health:
    get:
      summary: Health check endpoint
      description: Returns the API status
      responses:
        200:
          description: Healthy
          schema:
            type: object
            properties:
              status:
                type: string
            example:
              status: "ok"

