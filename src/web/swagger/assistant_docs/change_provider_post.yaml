---
swagger: '2.0'
info:
  title: Change Provider
  version: 1.0.0
parameters:
  - name: body
    in: body
    required: true
    schema:
      type: object
      required:
        - provider
      properties:
        provider:
          type: string
responses:
  200:
    description: Success
    schema:
      type: object
      properties:
        status:
          type: string
  400:
    description: Bad Request

